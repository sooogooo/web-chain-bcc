# 联合丽格 站点维护说明（chain.bccsw.cn / ec.chain.bccsw.cn）

> 本文档用于后续更新与维护。最后核验日期：2026-08-28。

## 1. 两站关系
- **源站 chain.bccsw.cn**：联合丽格集团官网，**西南中心**。联系人 **于晓冬**（135-0116-6830 / @sooogooo / yuxiaodong@beaucare.org），微信二维码已本地化 `/qr/sooogooo.png`。
- **新站 ec.chain.bccsw.cn**：由 chain **复刻**而来，**华东中心**。联系人 **辛宝音**（191-2142-8338 / 微信=手机号 19121428338 / xinbaoyin@beaucare.org），二维码为**本地** `/qr/xinbaoyin-wechat.png`。
- 两站页面、技术栈、结构**完全一致**，仅「中心名称、联系人、域名、二维码」不同。ec = chain 的“中心定制版”。

## 2. 技术栈
- 纯静态站，由 **openresty（1Panel 管理）** 承载。
- 前端：Tailwind 编译版 `/tw/*.css` + FontAwesome(bootcdn) + 本地 `js/`（echarts、chart.js、html2canvas、qrcode.min.js、bcc-qr.js、remixicon）。
- 品牌资源已**本地化**（不再依赖 docs.bccsw.cn）：`/logo.png`、`/favicon.png`、`/consultant.png`、chain `/qr/sooogooo.png`、ec `/qr/xinbaoyin-wechat.png`。
- 仍依赖的外部服务：cdn.tailwindcss.com、www.googletagmanager.com (GA G-2S3FKLTRMS)、`maxkb.ai.stemcell.gold`（智能助手 iframe/脚本）、cdn.bootcdn.net、`f.wps.cn`（「我要加盟」页 WPS 表单 iframe）。
- 「我要加盟」(join.html) 页表单为 **WPS 表单 iframe**：`https://f.wps.cn/g/lv4bWDSw/`（由 f.wps.cn 托管，非本站后端）。

## 3. 部署位置（服务器 talk.stemcell.gold）
| 项 | 源站 chain | 新站 ec |
|---|---|---|
| Web 根目录 | `/opt/1panel/apps/openresty/openresty/www/sites/chain.bccsw.cn/index` | `/opt/1panel/apps/openresty/openresty/www/sites/ec.chain.bccsw.cn/index` |
| nginx 配置 | `/opt/1panel/apps/openresty/openresty/conf/conf.d/chain.bccsw.cn.conf` | `/opt/1panel/apps/openresty/openresty/conf/conf.d/ec.chain.bccsw.cn.conf` |
| SSL | 各自 `ssl/` 目录（Let's Encrypt，1Panel 自动续期） | 同左 |

- 容器：`1Panel-openresty-yTI2`。
- 改配置后校验/重载：`docker exec 1Panel-openresty-yTI2 nginx -t` → `docker exec 1Panel-openresty-yTI2 nginx -s reload`。

## 4. Git 仓库（均为 SSH 推送，GitHub 用户 sooogooo）
| 站点 | 远程仓库 | 分支 | 状态 | 最后提交 |
|---|---|---|---|---|
| chain | `git@github.com:sooogooo/web-chain-bcc.git` | main | 与 origin 同步 | `ba18a0a` 本地化 docs 资源 |
| ec | `git@github.com:sooogooo/web-chain-bcc-ec.git` | main | 与 origin 同步 | `59fec86` 本地化 docs 资源 |

- 站点 Web 根目录本身就是 git 工作树；改文件即生效，`git commit/push` 做版本管理。身份：`sooogooo` / `sooogooo@gmail.com`。
- 运维注意：chain 的 `.git` 在 web 根内（已由 nginx 拦截不可公网访问）；ec 仓库由 root 初始化，曾需 `git config --global --add safe.directory <ec路径>`。
- 本机（Windows）装了 **gh CLI（GitHub CLI），已登录 sooogooo**，可用于创建仓库/API 操作。

## 5. ec 相对 chain 的“内容映射表”（复刻时替换）
后续给 ec 改内容、或复制新中心时按此套用：
| 原(chain) | 新(ec/新中心) |
|---|---|
| 于晓冬 | 辛宝音 |
| 135-0116-6830 | 191-2142-8338 |
| `tel:13501166830` | `tel:19121428338` |
| @sooogooo / sooogooo | 19121428338 |
| yuxiaodong@beaucare.org | xinbaoyin@beaucare.org |
| 西南中心 | 华东中心 |
| chain 本地二维码 `/qr/sooogooo.png` | ec 本地二维码 `/qr/xinbaoyin-wechat.png` |
| https://chain.bccsw.cn | https://ec.chain.bccsw.cn |

## 6. 移动端 UI 修复（已应用）
`#mobile-menu` 关闭态加 `visibility:hidden`、`.open` 用 `visibility:visible`，修复 `translateY(-100%)` 压到顶部导航导致 logo/标签重叠。
涉及 8 页：index、join、bcc-maps、bcc-maps-2、franchise-scene、glp-1、why-aesthetics-medicine、aesthetics-narrative-evolution。（chain 提交 `1a4ee24`；ec 直接改文件并已随初始提交。）

## 7. 安全加固（nginx 配置层，两站一致）
- `server_tokens off`；HSTS；`X-Content-Type-Options: nosniff`；`X-Frame-Options: SAMEORIGIN`；`Referrer-Policy: strict-origin-when-cross-origin`；`Permissions-Policy`；`Content-Security-Policy`（按站点实际 CDN 白名单放行，含 object-src none / base-uri / form-action / frame-ancestors；**frame-src 放行 maxkb.ai.stemcell.gold 与 f.wps.cn**——智能助手与「我要加盟」WPS 表单 iframe，缺则无法显示）。
- 拦截 `.git`、一切点文件；敏感扩展名（bak/py/md/xlsx/csv/json/sql/log/sh/ini/conf/pdf/env/zip/tgz/gz/tar/xz/7z/swp/swo/db/key/pem/crt/phar/sqlite/orig/dist/old）；`/backup`、`/docs` 目录。
- 静态资源 `try_files $uri =404;` + `expires 30d`。
- **重要**：`location ~* \.xml$` 让 sitemap 等 XML **干净返回（仅 HSTS，无 CSP）**，否则 Google 报「无法抓取」。

## 8. SEO / GEO（已做）
- **robots.txt**：显式放行 AI 搜索引擎：PerplexityBot、GPTBot、OAI-SearchBot、ChatGPT-User、ClaudeBot、anthropic-ai、Claude-Web、Google-Extended、Applebot-Extended、CCBot、Diffbot、FacebookBot、Amazonbot、Bytespider、cohere-ai；原有 Googlebot/Bingbot/百度/360/搜狗/神马等。
- **sitemap.xml**：13 条内容页（正确排除 404/Google 验证文件），`lastmod=2026-08-28`，changefreq/priority；robots.txt 引用各站域名 sitemap。
- 首页 title/OG/Twitter 加中心关键词：chain=西南中心、ec=华东中心。
- JSON-LD：Organization/WebSite/WebPage/Service/ContactPoint/FAQPage；FAQ 共 5 条（含 290+机构、覆盖100+城市、2030医美市场有望破6000亿 等统计）。

## 9. 维护操作速查
1. **改文章/内容**：编辑对应站点 Web 根下 `.html`；用相对路径（`bcc-maps.html`、`/js/...`、`/tw/...`）；域名引用用本站域名。
2. **改联系人/中心**：按第 5 节映射表在对应站点文件批量替换；换新二维码放到该站 `/qr/` 并更新引用。
3. **改 nginx**：编辑 `conf.d/{chain,ec}.chain.bccsw.cn.conf` → `docker exec ... nginx -t` → `nginx -s reload`。
4. **更新 sitemap**：改页面后更新 `sitemap.xml` 的 `<lastmod>`；保持 `.xml` 干净返回。
5. **推 GitHub**：到对应 Web 根目录 `git add -A && git commit -m "..." && git push origin main`（SSH）。
6. **回滚**：nginx 配置备份在 `conf.d/*.bak-sec-*`、`*.bak-xml-*`；站点内容用 git 回退。

## 10. 待办 / 注意
- **chain 的 sitemap 在 Google Search Console 需重新测试**：服务端已恢复干净（202、text/xml、无 CSP），但 GSC 缓存了此前「无法抓取」；请在 GSC 点「重新测试」（用 https:// 地址）。
- ec 无独立表单后端；join 表单为前端模拟。
- docs.bccsw.cn 已**本地化**（logo/favicon/二维码/consultant 配图），站点基本自足；仍依赖 tailwind CDN/gtag/maxkb 助手、`f.wps.cn`（加盟表单），若其异常会影响相应功能。
- 若想更严格 CSP（去掉 unsafe-inline），需重构内联脚本，改动较大。
- 两站 `Disallow: /images01/`、`/backup*`、`/*-202*`、`/tmp/`；AI 机器人 `Allow: /`。
